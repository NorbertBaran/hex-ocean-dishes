export const timer = {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "center",
    alignItems: "center",
    gap: {xs: 4, sm: 12}
}

export const clock = {
    minWidth: "200px",
    textAlign: "center"
}

export const requiredInfo = {
    width: '160px',
    color: '#fff',
    background: 'rgb(80, 80, 80)',
    padding: "10px 20px",
    borderRadius: "8px"
}